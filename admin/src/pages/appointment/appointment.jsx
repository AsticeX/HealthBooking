
import List from "../list/List";
import { appointmentColumns } from "../../datatablesource";
const Appointment = ({}) => {
  return (
    <List columns={appointmentColumns} />
  )
}

export default Appointment